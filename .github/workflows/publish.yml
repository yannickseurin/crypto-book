name: publish

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install mdbook, mdbook-katex, mdbook-toc, mdbook-mermaid
        run: cargo install mdbook mdbook-katex mdbook-toc mdbook-mermaid

      - name: Build html
        run: mdbook build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./book
